pull_request_rules:
  - name: automatic merge
    conditions:
      - base=main
      - "#approved-reviews-by>=1"
    actions:
      merge:
        method: merge
      dismiss_reviews: {}
      delete_head_branch: {}
